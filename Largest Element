#include <iostream>
using namespace std;

int main(){
    int arr[]={2, 5, 15, 7, 25};
    int n=5;
    int max=arr[0];

    for(int i=1;i<n;i++){
        if(arr[i]>max){
            max=arr[i];
        }
    }
    cout<<"Largest Element= "<<max;
}
